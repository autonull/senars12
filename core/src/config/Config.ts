/**
 * @deprecated Use `import type { ConfigSchema, ConfigEvent, ConfigCapability, ConfigView } from '@senars/util/config'` instead.
 */
export type {
  ConfigCapability,
  ConfigEvent,
  ConfigSchema,
  ConfigView,
} from '@senars/util/config';
