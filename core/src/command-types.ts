/**
 * @deprecated Will be removed in next major version.
 * Use `import type { CommandContext, CommandHandler, CommandDefinition } from '@senars/util'` instead.
 */
export type { CommandContext, CommandDefinition, CommandHandler } from '@senars/util';
