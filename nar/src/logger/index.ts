import type { LogLevel, LogEntry, LoggerConfig } from '@senars/core';
export type { LogLevel, LogEntry, LoggerConfig } from '@senars/core';
export { Logger, createLogger, defaultLogger } from '@senars/core';
