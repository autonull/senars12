/**
 * @deprecated Will be removed in next major version.
 * Use `import { AgentBridge } from '@senars/core'` (now re-exports from core/src/bridge/)
 */
export { AgentBridge, type BridgeDelta, type BridgeEvent } from './bridge/AgentBridge.js';
